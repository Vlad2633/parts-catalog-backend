# Parts Catalog — Backend

Серверна частина курсового проєкту  
**«Розробка особистого сайту-каталогу автозапчастин»**

Backend реалізує REST API для взаємодії з базою даних MongoDB та забезпечує
авторизацію, керування товарами, корзиною і замовленнями.

---

## Технологічний стек
- Node.js
- Express.js
- MongoDB
- Mongoose ODM
- JWT (JSON Web Token)
- ES Modules
- dotenv

---

## Основний функціонал
- Реєстрація та авторизація користувачів
- Розмежування ролей USER / ADMIN
- CRUD-операції для:
  - категорій
  - брендів
  - автозапчастин
- Оформлення та збереження замовлень
- Перегляд і обробка замовлень (ADMIN)
- Пошук, фільтрація та пагінація
- REST API для клієнтського React-додатку

---

## Архітектура
Проєкт побудований за шаровим підходом:
- **Controller Layer** — маршрути Express (обробка HTTP-запитів)
- **Domain Layer** — Mongoose-моделі (User, Category, Brand, Part, Order)
- **Repository Layer** — доступ до MongoDB через Active Record Pattern
- **Middleware** — авторизація, перевірка ролей, обробка помилок

---

## Вимоги до середовища
- Node.js **v18+**
- npm **v9+**
- MongoDB (local або MongoDB Atlas)

---

## Запуск проєкту з нуля

### 1. Клонування репозиторію
```bash
git clone https://github.com/<your-username>/parts-catalog-backend.git
cd parts-catalog-backend
```
### 2. Встановлення залежностей
```bash
npm install
```
### 3. Налаштування змінних середовища
Створити файл .env у корені проєкту:
```bash
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/parts_catalog
JWT_SECRET=super_secret_key
JWT_EXPIRES_IN=1d
```
### 4. Запуск сервера
```bash
npm run dev
```
Backend буде доступний за адресою:
👉 http://localhost:5000

## Авторизація (JWT)

- JWT генерується при логіні

- Токен зберігається на клієнті (localStorage)

- Для захищених маршрутів використовується middleware auth

- Logout реалізовано шляхом видалення токена на клієнті

## Тестові облікові записи
Admin
```bash
Email: admin@test.com
Password: 12345678
Role: ADMIN
```
User
```bash
Email: user@test.com
Password: 12345678
Role: USER
```
(Адміністратора можна створити напряму в БД або змінити роль користувача вручну)

## Основні API ендпоїнти
Auth

- POST /api/auth/register

- POST /api/auth/login

- GET /api/auth/me

Catalog

- GET /api/parts

- GET /api/parts/:id

- POST /api/parts (ADMIN)

- PUT /api/parts/:id (ADMIN)

- DELETE /api/parts/:id (ADMIN)

Orders

- POST /api/orders

- GET /api/orders (ADMIN)

## Обробка помилок
Backend повертає помилки у стандартизованому форматі:
```bash
{
  "message": "Error description"
}
```
HTTP-коди: 400, 401, 403, 404, 500.
## Демонстрація роботи
У додатку Б курсової роботи наведені приклади:

- успішної авторизації

- отримання каталогу

- створення замовлення

- адмін-операцій
